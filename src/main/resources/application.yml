spring:
   datasource:
      url: jdbc:postgresql://localhost:5433/blogdb
      username: postgres
      password: postgres
   jpa:
      hibernate:
         ddl-auto: update
      show-sql: true
      properties:
         hibernate:
            format_sql: true
   flyway:
      baseline-on-migrate: true
      enabled: false